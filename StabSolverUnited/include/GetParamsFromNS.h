#include <iostream>
// this is for future - NS output data file should contain 
// grid dimensions, ando also all basic parameters like Re_inf and L_ref
inline void GetParamsFromNS(std::string FieldName){
	FILE* file = fopen(&_FieldName[0],"rb");
	// 
	fclose(file);

}